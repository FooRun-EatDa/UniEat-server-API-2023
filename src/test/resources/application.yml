spring:
  profiles:
    include:
    - database
    - aws
    - auth
    - oauth2

  application:
    admin:
      enabled: false

  mvc:
    date-format: yyyy-MM-dd

server:
  shutdown: graceful
  ssl:
    enabled: false
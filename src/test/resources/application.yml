spring:
  profiles:
    include:
    - database
    - aws

  pid:
    file: .unieat-api.pid

  application:
    admin:
      enabled: false

  mvc:
    date-format: yyyy-MM-dd

server:
  shutdown: graceful
  ssl:
    enabled: false
